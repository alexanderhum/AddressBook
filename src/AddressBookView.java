public interface AddressBookView {
    /**
     * Updates the graphical representation of the model.
     * @param model The AddressBookModel to update.
     */
    void update(AddressBookModel model);
}
